{
    "version":2,
    "builds":[
        {
            "src": "./api/index.js",
            "use":"@vercel/node"
        }
    ],
    "routes":
       { 
        "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
        }
    
}